interface:
  display_name: "Task Flow"
  command: "/taskflow"
  install_path: "~/.codex/skills/taskflow/SKILL.md"
  short_description: "Explicit four-checkpoint quality workflow."
  default_prompt: "/taskflow Run this task through the four checkpoints without skipping validation."

policy:
  allow_implicit_invocation: false

notes:
  - "Install the skill in a directory named taskflow so manual slash invocation stays /taskflow."
