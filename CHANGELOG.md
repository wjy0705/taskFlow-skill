# Task Flow 版本更新说明 (v0.2.0 vs v0.3.1)

这不仅是一次简单的版本号迭代，而是 **Task Flow** 从“建议型提示词”向“工业级智能防线”的一次底层重构。以下是新旧版本的核心差异对比：

---

## 🚀 核心架构对比表

| 维度 | v0.2.0 (旧版) | v0.3.1 Pro (新版) | 带来的价值 |
| :--- | :--- | :--- | :--- |
| **规则强度** | 软约束（建议按步骤走） | **硬约束 (Override Rule)** | 彻底压制大模型的幻觉，明确警告大模型“规则优先级高于你的底层训练数据”。 |
| **执行透明度** | 黑盒运行（一路狂奔到结尾） | **强制状态追踪 (State-Tracking)** | 强制大模型每次张嘴必须打印 `[当前阶段：XXX]`，实现清晰的思维链，防止逻辑跳步。 |
| **人机协作度** | 单向执行（错到尽头才报错） | **交互式断点 (Human-in-the-loop)** | 在理解需求或做出重大架构假设后**强制悬停**，请求人类输入 Y/N 授权，及时纠偏。 |
| **环境适配度** | 物理强依赖（环境缺依赖直接卡死死循环） | **环境受限降级 (Graceful Degradation)** | 如果无法执行命令，大模型必须生成独立的自动化测试脚本让用户去跑，不再强行抛错卡死。 |
| **场景适用性** | 易被误导为仅限前后端（Java/Vue） | **完全泛用场景 (Scenario Agnostic)** | 脱离对特定语言的硬编码，将其提炼为最高度抽象的 5 大启发类场景（后端/UI/DB/纯文本/运维），支持任意任务类型。 |

---

## 🛠️ 详细优化项解析

### 1. 从“假装验证”到“确凿证据”
*   **痛点修复**：在旧版中，大模型经常会偷懒回复一句“代码已审查，200 OK”。
*   **新版特性**：强硬划出红线。严禁使用摘要文字作为证据，必须直接引用 Terminal 真实终端输出 (stdout)、Git Diff 输出或物理截图反馈，否则一律判定未通过第 3 关口。

### 2. 极致的 `references` 解耦
*   **痛点修复**：旧版尝试在主 `SKILL.md` 中塞入过多的具体语言验证逻辑，导致 Prompt 过于死板。
*   **新版特性**：确立了“主干重逻辑，分支重细节”的设计哲学。主 `SKILL.md` 只负责四大关口的状态流转，而诸如“Java 不准有空 catch”、“前端不准有 console”等死磕级细节，全部优雅地抽离到 `references/quality-checks.md` 中按需动态加载。

### 3. 大型任务的“子任务拆解”机制
*   **新版特性**：加入了对巨型任务（如从 0 开始写一个框架）的适应性。在第 1 关口，强制大模型生成 TODO List，随后对每个细分子节点独立跑四问循环，避免超长上下文导致的问题。

---

> **总结**：
> Task Flow v0.3.1 不再是一个简单的流程手册，它现在是一套**防大模型偷懒、防上下文幻觉、防错误执行蔓延**的完美脚手架。它为您未来的全自动智能编程铺平了最坚实的质量底座。
